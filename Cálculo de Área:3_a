#include <stdio.h>

int main()
{
    float area, raio;
    
         
  printf("calcule a area de um circulo:");
       scanf ("%f", &raio);
    
  area = 3.14 * (raio * raio);
      printf("a area e : %.2f", area);
  
    return 0;
}

